<h2 class="title"><%=l(:label_version)%></h2>

<%= labelled_form_for @version, :method => 'put' do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<%= submit_tag l(:button_save),:class=>'greenBtn' %>
<% end %>

<%# content_for :sidebar do %>

<%#= render :partial => 'layouts/projects_sidebar' %>

<%# end %>

