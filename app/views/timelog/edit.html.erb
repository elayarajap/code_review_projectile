<h2 class="title"><%= l(:label_spent_time) %></h2>

<%= labelled_form_for @time_entry, :url => project_time_entry_path(@time_entry.project, @time_entry) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_save), :class=>'greenBtn' %>
<% end %>
<%= render :partial => 'projects/add_sprint_close_project' %>
<script>
jQuery(document).ready(function(){
$("#time_entry_issue_id").attr('readonly', true);
});
</script>
